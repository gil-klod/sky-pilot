{
  "name": "sky-pilot",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy:sim",
    "copy:sim": "cp -r sim dist/sim",
    "preview": "vite preview",
    "export:sim": "bash scripts/export-sim.sh",
    "export:sim:v2": "bash scripts/export-sim-v2.sh"
  },
  "dependencies": {
    "three": "^0.172.0"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
